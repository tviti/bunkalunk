[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "bunkalunk"
version = "0.0.0"
requires-python = ">=3.9"
dependencies = [
  "fitdecode",
  "h5py",
  "requests"
]

[tool.setuptools.packages.find]
where = ["."]
include = ["bunkalunk*"]
